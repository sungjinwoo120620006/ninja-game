

        #self.img = pygame.image.load('data/images/clouds/cloud_1.png')
        # dòng lệnh tải hình ảnh lên màn hình
        #self.img.set_colorkey((0,0,0))
        # dòng lệnh để lọc đi viền ngoài của hình ảnh
        #self.img_pos = [160,260]


        #self.collision_area = pygame.Rect(50, 50, 300, 50)

        #img_r = pygame.Rect(self.img_pos[0], self.img_pos[1], self.img.get_width(), self.img.get_height())
            #if img_r.colliderect(self.collision_area):# đưa ra điều kiện khi hình ảnh chạm vào khu vực chỉ định
            #    pygame.draw.rect(self.screen, (0, 100, 255), self.collision_area)
                # tô màu cho ô chữ nhật khi vật chưa
                # chạm vào
            #else:
            #    pygame.draw.rect(self.screen, (0, 50, 155), self.collision_area)
                # tô màu cho ô chữ nhật khi vâtj chạm vào
            #self.img_pos[1] += (self.movement[1] - self.movement[0] ) *5
            # dòng lệnh để nó tự động cộng khi mình di chuyển key và giảm
            #self.screen.blit(self.img,self.img_pos)
            # dòng lệnh để vẽ hình ảnh lên màn hình

